# Leave unset in local dev — Vite proxies /api and /uploads to localhost:5000 (no CORS).
# VITE_API_BASE_URL=http://localhost:5000

# Production (Netlify): point at the Render backend
# VITE_API_BASE_URL=https://dynesis-tech-backend.onrender.com
